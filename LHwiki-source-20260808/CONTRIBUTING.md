# 为 LHwiki 贡献代码

感谢参与。提交代码前请：

1. 从新分支开始修改，不要提交 `node_modules`、`.env`、`.dev.vars`、备份、日志或任何真实密钥。
2. 保持前端无外部运行时 CDN 依赖，确保中国大陆常见校园网络可以加载。
3. 数据库结构变化应新增 `cloudbase/migrations/` 迁移，不回改已执行的历史迁移。
4. 运行 `npm test`，确认所有测试通过。
5. Pull Request 中说明用户可见变化、权限边界和人工验证步骤。

涉及学号、未公开投稿、审核记录或真实人物评价的数据，不得加入测试夹具或 Issue。
